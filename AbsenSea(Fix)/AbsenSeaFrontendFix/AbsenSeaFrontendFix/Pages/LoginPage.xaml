<Page x:Class="AbsenSeaFrontendFix.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AbsenSeaFrontendFix.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="LoginPage">

    <Page.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="#F3F4F6">
        <!-- Left Panel - Branding -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Side - Brand Panel -->
        <Border Grid.Column="0" Background="#1E3A8A">
            <StackPanel VerticalAlignment="Center" Margin="60">
                <TextBlock Text="⚓" FontSize="72" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBlock Text="AbsenSea" FontSize="48" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Ship Crew Attendance System" FontSize="18" Foreground="#93C5FD" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <TextBlock Text="Monitor crew safety equipment compliance in real-time" 
                           FontSize="14" 
                           Foreground="#BFDBFE" 
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           HorizontalAlignment="Center" 
                           Margin="0,30,0,0"
                           MaxWidth="300"/>
            </StackPanel>
        </Border>

        <!-- Right Side - Login Form -->
        <Border Grid.Column="1" Background="White">
            <StackPanel VerticalAlignment="Center" Margin="80,0">
                <TextBlock Text="Welcome Back" FontSize="32" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,8"/>
                <TextBlock Text="Sign in to your ship account" FontSize="14" Foreground="#6B7280" Margin="0,0,0,40"/>

                <!-- Ship ID Input -->
                <TextBlock Text="Ship ID" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
                <TextBox x:Name="ShipIdTextBox" 
                         Style="{StaticResource ModernTextBox}"
                         Margin="0,0,0,20"/>

                <!-- Password Input -->
                <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,8"/>
                <PasswordBox x:Name="PasswordBox" 
                             Style="{StaticResource ModernPasswordBox}"
                             Margin="0,0,0,10"/>

                <!-- Remember Me & Forgot Password -->
                <Grid Margin="0,0,0,30">
                    <CheckBox Content="Remember me" FontSize="13" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <TextBlock Text="Forgot password?" 
                               FontSize="13" 
                               Foreground="#2563EB" 
                               HorizontalAlignment="Right" 
                               VerticalAlignment="Center"
                               Cursor="Hand">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Login Button -->
                <Button Content="Sign In" 
                        Style="{StaticResource ModernButton}"
                        Click="LoginButton_Click"
                        Margin="0,0,0,20"/>

                <!-- Register Link -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Don't have an account? " FontSize="13" Foreground="#6B7280"/>
                    <TextBlock Text="Register" 
                               FontSize="13" 
                               Foreground="#2563EB" 
                               FontWeight="SemiBold"
                               Cursor="Hand"
                               MouseLeftButtonDown="RegisterLink_Click">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="TextDecorations" Value="Underline"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
